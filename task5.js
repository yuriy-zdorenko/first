let temp

while (true) {
  temp = parseInt(prompt('Температура в градусах Цельсія:'))

  if (isNaN(b)) {
    alert('Введіть число.')
  } else {
    const fahrenheit = (9 / 5) * temp + 32
    alert(`${fahrenheit}℉`)
    break
  }
}
